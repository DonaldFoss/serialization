define(['./primitives', './generic', './structure', './group', './list/index', './any', './Text', './Data', './zero'], function (
           primitives,     generic,     structure,     group,     lists,          any,     Text,     Data,     zero) {

    return {
        primitives : primitives,
        generic : generic,
        structure : structure,
        group : group,
        lists : lists,
        any : any,
        Text : Text,
        Data : Data,
        zero : zero
    };
});
